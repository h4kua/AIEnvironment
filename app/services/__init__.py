# Direct imports are preferred over this re-export layer.
# Use:  from app.services.prediction_service import predict_realtime
# Use:  from app.realtime_native.inference import predict_realtime_native
